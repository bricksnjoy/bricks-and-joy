# Every value settleup.tech needs at runtime. Copy to settleup.env and fill in.
# Not in git — see .gitignore.
#
# DATABASE_URL=postgres://user:password@host.docker.internal:5432/settleup
# SESSION_SECRET=
